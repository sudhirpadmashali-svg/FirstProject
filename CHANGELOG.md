# Changelog

All notable changes to this project are documented here. Entries below this
point are generated by `generate_release_notes.py` (see the
`release-notes.yml` workflow) each time a `vX.Y.Z` tag is pushed — new
releases are prepended above older ones.

<!-- New release sections are inserted below this line -->
